[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "skyline-qc"
version = "0.1.0"
description = "Simple tools to clean up data from Skyline"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    {name = "Thanadol Sutantiwanichkul", email = "khunkoei@gmail.com"},
]
license = {text = "MIT"}
dependencies = [
    "requests",
    "pandas",
    "numpy",
    "lxml",
    "matplotlib",
    "seaborn",
    "statsmodels",
    "sdrf-pipelines[ontology]",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.urls]
Homepage = "https://github.com/thanadol-git/skyline_qc"
Issue = "https://github.com/thanadol-git/skyline_qc/issues"